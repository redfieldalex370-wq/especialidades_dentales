Options -MultiViews
RewriteEngine On
RewriteBase /

# Vite SPA: permite abrir /doctor y /pantalla directamente en Hostinger.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
